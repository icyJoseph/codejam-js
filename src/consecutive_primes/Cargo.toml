[package]
edition = "2021"
name = "consecutive_primes"
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]

[[bin]]
name = "consecutive_primes"
path = "src/consecutive_primes.rs"
